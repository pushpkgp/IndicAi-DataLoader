# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: indic-ai-data-loader
spec:
  selector:
    app: indic-ai-data-loader
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 30080
  type: NodePort